# This is a Databricks asset bundle definition for bookstore_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: bookstore_dab
  uuid: 09bc22ec-280d-49cd-bb14-de320f6e6c95

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: http://127.0.0.1:7073
    variables:
      catalog: ${var.catalog_name}
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: http://127.0.0.1:7073
      # We explicitly deploy to /Workspace/Users/jorgecardenasg1995@outlook.com to make sure we only have a single copy.
      root_path: /Workspace/Users/jorgecardenasg1995@outlook.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: ${var.catalog_name}
      schema: prod
    permissions:
      - user_name: jorgecardenasg1995@outlook.com
        level: CAN_MANAGE
